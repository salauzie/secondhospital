%h1 Patients

= form_tag clinic_patients_path, method: :get, role: "search" do
	= text_field_tag 'q', params[:q], id: "srch-term", placeholder: "Search" 
	= submit_tag "Search"

- @patients.each do |f|
	= link_to f.first_name + " " + f.last_name, clinic_patient_path(@clinic, f)
	%br/
%br/
